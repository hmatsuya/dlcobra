# exp016: Focal Loss for policy and value heads (base: exp015, InceptionNeXt depth=10)
model:
  network: dlshogi.experiments.exp016_focal_loss.model.PolicyValueNetwork
  focal_alpha: 0.25
  focal_gamma: 2.0
