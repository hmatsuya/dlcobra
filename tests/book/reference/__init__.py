"""Independent reference implementations used only to cross-check the real
``dlshogi.book`` modules in property tests (tasks 5.1, 14.1).

Nothing in ``tests/book/reference`` imports from ``dlshogi.book``; each
module here is written directly from the requirements text and reviewed
against the requirements, not against the implementation it cross-checks.
See each module's own docstring for the specific requirement it derives
from.
"""
